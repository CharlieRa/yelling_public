'use strict';

describe('yelling.perfil module', function() {

  beforeEach(module('yelling.perfil'));

  describe('view2 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('perfilCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});
